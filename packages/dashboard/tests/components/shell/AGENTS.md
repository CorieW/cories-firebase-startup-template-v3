# packages/dashboard/tests/components/shell

Shell-component tests for authenticated navigation and layout behavior.

## Directories

- None.

## Files

- `SignedInSidebar.test.tsx`: Tests signed-in sidebar navigation, billing groups, and org state.

## Writing Rules

- Keep shell tests focused on navigation state, auth-driven rendering, and other layout behavior shared across routes.
- Update file when additional shell suites are added or existing shell test helpers are introduced here.
